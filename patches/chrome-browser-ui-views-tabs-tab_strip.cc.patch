diff --git a/chrome/browser/ui/views/tabs/tab_strip.cc b/chrome/browser/ui/views/tabs/tab_strip.cc
index 95da63fca25f0045395337b9ac01cd7b4d96d7b6..7467e9d4c6060863a016eee5d34ff285603d204b 100644
--- a/chrome/browser/ui/views/tabs/tab_strip.cc
+++ b/chrome/browser/ui/views/tabs/tab_strip.cc
@@ -350,7 +350,7 @@ class TabStrip::TabDragContextImpl : public TabDragContext,
            event.type() == ui::EventType::kGestureScrollBegin))
         << "Event type must be suitable for starting a drag.";
 
-    drag_controller_ = std::make_unique<TabDragController>();
+    drag_controller_ = std::make_unique<BraveTabDragController>();
 
     // !!! Init may delete `drag_controller_` on some platforms. !!!
     // Init takes capture, which on some platforms may reenter Chrome, the
@@ -591,6 +591,7 @@ class TabStrip::TabDragContextImpl : public TabDragContext,
 
   std::vector<gfx::Rect> CalculateBoundsForDraggedViews(
       const std::vector<TabSlotView*>& views) override {
+    BRAVE_TAB_DRAG_CONTEXT_IMPL_CALCULATE_BOUNDS_FOR_DRAGGED_VIEWS
     CHECK(!views.empty()) << "The views vector must not be empty.";
 
     std::vector<gfx::Rect> bounds;
@@ -940,6 +941,7 @@ class TabStrip::TabDragContextImpl : public TabDragContext,
         continue;
       }
 
+      BRAVE_TAB_DRAG_CONTEXT_IMPL_CALCULATE_INSERTION_INDEX
       // If there's a group header here, and we're dragging a group, we might
       // end up on either side of that header. Check both cases to find the
       // best option.
@@ -1486,7 +1488,7 @@ void TabStrip::SetSelection(const ui::ListSelectionModel& new_selection) {
       const tab_groups::TabGroupId new_group = selected_tab->group().value();
       // If the tab that is about to be selected is in a collapsed group,
       // automatically expand the group.
-      if (IsGroupCollapsed(new_group)) {
+      if (IsGroupCollapsed(new_group) && IsTabStripEditable()) {
         // If the group is being dragged, do not expand it.
         if (drag_context_->GetDragController() &&
             drag_context_->GetDragController()->group_header_id() ==
